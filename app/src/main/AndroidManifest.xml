<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unisparc.morblood">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.RecipientRequestAccept"></activity>
        <activity android:name=".activities.AcceptOrNotActivity" />
        <activity android:name=".activities.RecipientDetailActivity" />
        <activity android:name=".activities.DonordetailActivity" />
        <activity android:name=".activities.data_onboard" />
        <activity android:name=".Onboard">
            android:configChanges="orientation|screenSize"
            android:windowSoftInputMode="adjustPan"
        </activity>
        <activity android:name=".LoginRegister" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".donor_activity" />
        <activity android:name=".recipient_activity" />
        <activity android:name=".user_details" />
    </application>

</manifest>